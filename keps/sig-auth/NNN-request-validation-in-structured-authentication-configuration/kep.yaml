title: Request Validation in StructuredAuthenticationConfiguration
kep-number: NNNN
authors:
  - "@modheer"
owning-sig: sig-auth
participating-sigs:
  - sig-api-machinery
status: provisional
creation-date: 2024-09-05
reviewers:
  - TBD
approvers:
  - TBD
prr-approvers:
  - TBD

# The target maturity stage in the current dev cycle for this KEP.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.32"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "v1.32"
  beta: "v1.33"
  stable: "v1.34"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: StructuredAuthenticationConfiguration
    components:
      - kube-apiserver
disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - authentication_request_validation_total
  - authentication_request_validation_duration_seconds
  - authentication_request_validation_failures_total
